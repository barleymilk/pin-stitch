{
  "name": "pin-stitch",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace @pin-stitch/web",
    "dev:admin": "npm run dev --workspace @pin-stitch/admin",
    "build": "npm run build --workspace @pin-stitch/web && npm run build --workspace @pin-stitch/admin",
    "lint": "npm run lint --workspace @pin-stitch/web && npm run lint --workspace @pin-stitch/admin",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "prettier": "^3.8.3"
  }
}
